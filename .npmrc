# Save exact versions to avoid unexpected updates
save-exact=true

# Don't save dev dependencies in package.json
save-dev=false

# Max old space size for Node.js - helps with memory issues
node-options=--max-old-space-size=4096

# Disable audit to speed up installations
audit=false

# Use fine-grained logging for debugging
loglevel=warn 